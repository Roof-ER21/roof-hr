# Use Node.js 20 via .nvmrc/.node-version detection
# Don't specify nixPkgs - let nixpacks auto-detect from .node-version file

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production node dist/index.js"
